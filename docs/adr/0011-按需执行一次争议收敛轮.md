# 按需执行一次争议收敛轮

修订意见后若至少两名参与者独立标记关键未决争议，应用默认追加一次严格限定范围的争议收敛轮；用户或 `auto|always|never` 策略可以覆盖触发判断。`always` 模式缺少有效信号时允许报告 Agent从修订意见提出带来源的候选争议，但仍无具体主题则带警告跳过。该轮不投票、不强制共识，也不递归追加，而是把每项争议整理为已解决、条件性一致或仍未解决后进入报告阶段，从而在提高困难问题质量的同时保持可预测的成本和终止条件。
