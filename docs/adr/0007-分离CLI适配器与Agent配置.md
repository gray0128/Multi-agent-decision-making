# 分离 CLI 适配器与 Agent 配置

应用将 CLI 调用能力与可参与审议的身份分离：CLI 适配器负责一种 CLI 运行时的安全调用和事件归一化，Agent 配置则引用适配器并指定名称、模型或服务商、可执行路径、参数及角色指令。同一适配器可以注册多个 Agent 配置，例如分别使用 DeepSeek V4 Pro 和 MiniMax-M3 的两个 Pi 参与者。该分层避免把 CLI、模型和参与者身份混为一谈，并支持在不复制适配代码的情况下扩展不同模型组合；代价是配置校验和健康检查需要同时覆盖运行时与模型参数。
