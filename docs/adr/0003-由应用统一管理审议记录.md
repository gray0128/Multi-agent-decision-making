# 由应用统一管理审议记录

应用是审议记录的唯一权威来源；每次发言都通过对应 CLI 适配器启动新的非交互进程，并显式传入原问题、当前阶段要求和所需历史，而不使用各 CLI 运行时的 `resume` 或 `continue` 私有会话。后续调用优先接收完整记录；当预计超过任一参与者声明的上下文预算时，由报告 Agent 生成一份保留原问题、用户指导、关键论点、分歧及来源的审议摘要，所有后续参与者使用同一份摘要，完整原始输出仍保留在审议档案中。该选择确保异构参与者基于同一可见上下文工作，使审议可持久化、检查和重放，但会增加输入 token，且摘要可能损失部分细节。
