# 由审议控制台发起独立审议进程

`mad serve` 提供的网页从只读观察页升级为审议控制台，可以收集审议议题与按次设置、启动独立审议进程，并转发候选方案修改、方案确认和检查点响应；审议进程仍是运行状态、规则校验和正式档案的唯一所有者，控制台关闭或重启不终止已启动的审议。该决定修订 ADR-0012 与 ADR-0014 中“网页不能发起审议、命令行是主要发起入口”的范围边界，以增加启动协议、进程发现和幂等控制的代价，换取完整的本地网页发起体验，同时避免把审议引擎嵌入服务进程或仅生成一条需要复制执行的 CLI 命令。
