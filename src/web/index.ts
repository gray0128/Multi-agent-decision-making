export const WEB_ASSET_VERSION = 1;

export const INDEX_HTML: string = `<!doctype html>
<html lang="zh-CN">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>MAD 审议观察台</title><link rel="stylesheet" href="/styles.css"></head>
<body>
  <div class="grain"></div>
  <header><div><span class="eyebrow">LOCAL DELIBERATION LEDGER / 只读观察台</span><h1>MAD<span>·</span>审议档案</h1></div><div id="connection" class="signal">连接中</div></header>
  <main>
    <aside><div class="aside-title"><b>档案索引</b><button id="refresh" title="刷新">↻</button></div><nav id="archive-list"></nav></aside>
    <section id="detail" class="empty"><div class="index-mark">01</div><h2>选择一份审议档案</h2><p>历史、实时阶段与当前检查点都会在这里展开。</p></section>
  </main>
  <script type="module" src="/app.js"></script>
</body></html>`;

export const STYLES_CSS: string = `
:root{--ink:#171914;--paper:#e8e3d6;--muted:#777568;--rule:#393b32;--acid:#d7ff3f;--ember:#e6552f;--panel:#d8d2c4}*{box-sizing:border-box}html,body{margin:0;min-height:100%;background:var(--paper);color:var(--ink)}body{font-family:Menlo,Monaco,"Courier New",monospace}.grain{position:fixed;inset:0;pointer-events:none;opacity:.08;background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='2' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.45'/%3E%3C/svg%3E")}header{height:138px;border-bottom:2px solid var(--ink);display:flex;align-items:flex-end;justify-content:space-between;padding:24px 32px 20px;background:var(--ink);color:var(--paper)}.eyebrow{font-size:10px;letter-spacing:.15em;color:var(--acid)}h1{font-family:"Iowan Old Style","Palatino Linotype",serif;font-size:48px;line-height:.85;margin:14px 0 0;font-weight:600;letter-spacing:-.04em}h1 span{color:var(--ember)}.signal{font-size:11px;border:1px solid var(--acid);padding:8px 11px;color:var(--acid);text-transform:uppercase}.signal.off{border-color:var(--ember);color:var(--ember)}main{display:grid;grid-template-columns:320px minmax(0,1fr);min-height:calc(100vh - 138px)}aside{border-right:2px solid var(--ink);background:var(--panel)}.aside-title{height:54px;border-bottom:1px solid var(--rule);display:flex;align-items:center;justify-content:space-between;padding:0 18px;text-transform:uppercase;font-size:11px;letter-spacing:.1em}button{font:inherit;background:var(--acid);border:1px solid var(--ink);color:var(--ink);padding:8px 11px;cursor:pointer;box-shadow:3px 3px 0 var(--ink)}button:hover{transform:translate(1px,1px);box-shadow:2px 2px 0 var(--ink)}nav button{display:block;width:100%;text-align:left;background:transparent;border:0;border-bottom:1px solid #a9a596;padding:16px 18px;box-shadow:none}nav button:hover,nav button.active{transform:none;box-shadow:inset 5px 0 0 var(--ember);background:var(--paper)}nav small{display:block;color:var(--muted);margin-top:8px}.status{display:inline-block;font-size:9px;border:1px solid;padding:3px 5px;margin-right:6px;text-transform:uppercase}.status.completed{background:var(--acid)}.status.running,.status.waiting_checkpoint{background:var(--ember);color:white}#detail{padding:30px 4vw 80px;max-width:1120px}.empty{position:relative;padding-top:15vh!important}.index-mark{font-family:"Iowan Old Style",serif;font-size:110px;color:#c8c1b1;line-height:.8;position:absolute;right:4vw;top:8vh}h2{font-family:"Iowan Old Style","Palatino Linotype",serif;font-size:36px;letter-spacing:-.025em;margin:12px 0}.meta{display:flex;flex-wrap:wrap;gap:8px 22px;border-top:2px solid var(--ink);border-bottom:1px solid var(--ink);padding:12px 0;margin:20px 0 28px;font-size:11px}.meta b{color:var(--muted);font-weight:normal}.checkpoint{border:2px solid var(--ember);padding:18px;margin:0 0 28px;background:#f1d7c7;box-shadow:8px 8px 0 var(--ink)}.checkpoint h3{margin-top:0}.checkpoint input{width:100%;font:inherit;border:1px solid var(--ink);background:var(--paper);padding:10px;margin:0 0 12px}.actions{display:flex;gap:10px;flex-wrap:wrap}.actions button[data-action=cancel]{background:var(--ember);color:white}.section-label{font-size:10px;letter-spacing:.17em;text-transform:uppercase;color:var(--muted);margin:36px 0 10px}.participants{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));border-top:1px solid var(--ink);border-left:1px solid var(--ink)}.participant{border-right:1px solid var(--ink);border-bottom:1px solid var(--ink);padding:13px}.participant b{display:block;margin-bottom:7px}.participant small{color:var(--muted)}.report{font-family:"Iowan Old Style","Palatino Linotype",serif;font-size:18px;line-height:1.65;max-width:820px}.report h1,.report h2,.report h3{color:var(--ink);font-family:inherit;line-height:1.1;margin-top:1.5em}.report h1{font-size:38px}.report h2{font-size:27px;border-bottom:1px solid;padding-bottom:6px}.report code{font-family:Menlo,monospace;background:#d2ccbd;padding:.1em .3em}.report pre{overflow:auto;background:var(--ink);color:var(--paper);padding:16px}.events{font-size:11px;border-left:2px solid var(--ink);padding-left:15px}.event{display:grid;grid-template-columns:150px 1fr;gap:12px;padding:7px 0;border-bottom:1px dotted #aaa493}.event time{color:var(--muted)}@media(max-width:760px){header{height:112px;padding:18px}h1{font-size:36px}main{display:block}aside{border-right:0;border-bottom:2px solid;max-height:240px;overflow:auto}#detail{padding:24px 18px}.index-mark{display:none}}
`;

export const APP_JS: string = `
const tokenKey='mad-observer-token';const fragment=new URLSearchParams(location.hash.slice(1));if(fragment.get('token')){sessionStorage.setItem(tokenKey,fragment.get('token'));history.replaceState(null,'',location.pathname)}const token=sessionStorage.getItem(tokenKey)||'';const headers=()=>({Authorization:'Bearer '+token});const list=document.querySelector('#archive-list');const detail=document.querySelector('#detail');const signal=document.querySelector('#connection');let selected='';let eventAbort;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function markdown(source){let safe=esc(source);safe=safe.replace(/^### (.+)$/gm,'<h3>$1</h3>').replace(/^## (.+)$/gm,'<h2>$1</h2>').replace(/^# (.+)$/gm,'<h1>$1</h1>').replace(/\*\*(.+?)\*\*/g,'<strong>$1</strong>').replace(/\`([^\`]+)\`/g,'<code>$1</code>').replace(/^(?:- |\* )(.+)$/gm,'<li>$1</li>').replace(/\n{2,}/g,'</p><p>').replace(/\n/g,'<br>');return '<p>'+safe+'</p>'}
async function api(path,options={}){const response=await fetch(path,{...options,headers:{...headers(),...(options.headers||{})}});if(!response.ok)throw new Error(await response.text()||response.statusText);return response}
async function refresh(){try{const data=await (await api('/api/deliberations')).json();signal.textContent='LOCAL / AUTHENTICATED';signal.classList.remove('off');list.innerHTML=data.map(x=>'<button data-id="'+esc(x.id)+'" class="'+(x.id===selected?'active':'')+'"><span class="status '+esc(x.status)+'">'+esc(x.status)+'</span>'+esc(x.question)+'<small>'+esc(x.mode)+' · '+esc(x.createdAt)+'</small></button>').join('');list.querySelectorAll('button').forEach(b=>b.onclick=()=>openArchive(b.dataset.id))}catch(error){signal.textContent='DISCONNECTED';signal.classList.add('off');list.innerHTML='<button>认证失败或服务不可用</button>'}}
async function openArchive(id){selected=id;eventAbort?.abort();const data=await (await api('/api/deliberations/'+encodeURIComponent(id))).json();const m=data.manifest,s=data.state;detail.className='';detail.innerHTML='<span class="status '+esc(s.status)+'">'+esc(s.status)+'</span><h2>'+esc(m.question)+'</h2><div class="meta"><span><b>ID</b> '+esc(m.id)+'</span><span><b>MODE</b> '+esc(m.mode)+'</span><span><b>CALLS</b> '+esc(s.callAttempts)+' / '+esc(m.plan.limits.maxCalls)+'</span><span><b>CREATED</b> '+esc(m.createdAt)+'</span></div>'+checkpoint(data.checkpoint,id)+'<div class="section-label">Participants / 来源与角色</div><div class="participants">'+m.plan.participants.map(p=>'<div class="participant"><b>'+esc(p.id)+'</b><small>'+esc(p.invocation.cli)+' / '+esc(p.invocation.preset)+'</small><p>'+esc(p.role)+'</p></div>').join('')+'</div><div class="section-label">Transcript / 已提交权威记录</div><div id="transcript">'+transcriptHtml(data.transcript)+'</div><div class="section-label">Authoritative outcome / 共同成果</div><article class="report">'+(data.report?markdown(data.report):'<p>尚未生成最终报告。</p>')+'</article><div class="section-label">Lifecycle / 实时事件</div><div id="events" class="events">'+data.events.map(eventHtml).join('')+'</div>';detail.querySelectorAll('[data-checkpoint-action]').forEach(b=>b.onclick=()=>respond(id,b.dataset.checkpointId,b.dataset.checkpointAction));await refresh();stream(id,data.events.length)}
function checkpoint(value,id){if(!value)return '';return '<section class="checkpoint"><h3>当前检查点</h3><p>'+esc(value.kind)+' · '+esc(value.summary||'等待响应')+'</p><input id="checkpoint-guidance" placeholder="可选：补充指导" aria-label="检查点指导"><div class="actions">'+value.actions.map(a=>'<button data-checkpoint-action="'+esc(a)+'" data-checkpoint-id="'+esc(value.checkpointId)+'">'+esc(a)+'</button>').join('')+'</div></section>'}
function transcriptHtml(records){return records.map(r=>'<article class="participant"><small>'+esc(r.stage)+' · '+esc(r.agentId)+' · '+esc(r.invocation?.cli)+'/'+esc(r.invocation?.preset)+'</small><div class="report">'+markdown(r.content)+'</div></article>').join('')||'<p>尚无已提交发言。</p>'}
function eventHtml(e){return '<div class="event"><time>'+esc(e.at)+'</time><span>'+esc(e.type)+'</span></div>'}
async function respond(id,checkpointId,action){const guidance=document.querySelector('#checkpoint-guidance')?.value||'';await api('/api/checkpoints/'+encodeURIComponent(id)+'/respond',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({checkpointId,action,guidance})});await openArchive(id)}
async function refreshTranscript(id){const data=await (await api('/api/deliberations/'+encodeURIComponent(id))).json();const target=document.querySelector('#transcript');if(target)target.innerHTML=transcriptHtml(data.transcript)}
async function stream(id,after){eventAbort=new AbortController();try{const response=await fetch('/api/deliberations/'+encodeURIComponent(id)+'/events?after='+after,{headers:headers(),signal:eventAbort.signal});const reader=response.body.getReader(),decoder=new TextDecoder();let buffer='';while(true){const part=await reader.read();if(part.done)break;buffer+=decoder.decode(part.value,{stream:true});const frames=buffer.split('\n\n');buffer=frames.pop();for(const frame of frames){const line=frame.split('\n').find(x=>x.startsWith('data:'));if(line){const event=JSON.parse(line.slice(5));document.querySelector('#events')?.insertAdjacentHTML('beforeend',eventHtml(event));if(event.type==='invocation.committed')refreshTranscript(id);if(event.type==='deliberation.completed'||event.type.startsWith('checkpoint.')){await openArchive(id);return}if(event.type.startsWith('deliberation.'))refresh()}}}}catch(error){if(error.name!=='AbortError')signal.classList.add('off')}}
document.querySelector('#refresh').onclick=refresh;refresh();setInterval(refresh,5000);
`;
